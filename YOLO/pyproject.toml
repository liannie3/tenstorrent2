[project]
name = "yolo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rianhickey", email = "rch27@tamu.edu" }
]
requires-python = ">=3.10"
dependencies = [
    "torch>=2.14.0",
    "torchvision>=0.29.0",
    "ultralytics>=8.4.123",
]

[project.scripts]
yolo = "yolo:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
